package powerful.powers.client;

/**
 * Superseded by AbilityKeybind.InputListener.
 * Kept as an empty class to avoid removing a file that may be referenced
 * elsewhere, but all logic has been moved to AbilityKeybind.
 */
public class ClientEvents {
    // Intentionally empty — see AbilityKeybind for keybind + charge tracking.
}
